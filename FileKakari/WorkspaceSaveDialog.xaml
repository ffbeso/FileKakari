<Window x:Class="FileKakari.WorkspaceSaveDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save as Workspace" Height="160" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource AppBackgroundBrush}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="NameLabel"
                   Text="Workspace Name"
                   Foreground="{DynamicResource TextBrush}"
                   Margin="0,0,0,6" />

        <TextBox x:Name="NameBox"
                 Grid.Row="1"
                 VerticalContentAlignment="Center"
                 Padding="8,0"
                 Height="32"
                 Background="{DynamicResource ControlBackgroundBrush}"
                 Foreground="{DynamicResource TextBrush}"
                 BorderBrush="{DynamicResource BorderBrush}"
                 CaretBrush="{DynamicResource TextBrush}" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="SaveButton" Content="Save" Width="80" Margin="0,0,8,0" IsDefault="True" Click="Save_Click" Style="{StaticResource FileKakariButtonStyle}" />
            <Button x:Name="CancelButton" Content="Cancel" Width="80" IsCancel="True" Style="{StaticResource FileKakariButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
